<p id="notice"><%= notice %></p>

<h1>Listing Users</h1>
<% if notice %>
  <p id="notice"><%= notice %></p>
➤ <% end %>
<table class ="table table-hover">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @users.each do |user| %>
      <tr>
        <div class="container-fluid">
          <td><%= user.name %></td>
          <td><btn-default><%= link_to 'Show', user %></btn></td>
          <td><%= link_to 'Edit', edit_user_path(user) %></td>
          <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </div>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path, class: 'btn btn-success' %></li>
<% #end %>
